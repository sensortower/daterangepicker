class DateRange
  constructor: (title, startDate, endDate) ->
    @title = title
    @startDate = startDate
    @endDate = endDate

class AllTimeDateRange extends DateRange
class CustomDateRange extends DateRange
